apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  labels:
    {{- include "pgbouncer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "pgbouncer.selectorLabels" . | nindent 4 }}
  ports:
    - name: pgbouncer
      port: 6432
      targetPort: 6432
