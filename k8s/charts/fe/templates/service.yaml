apiVersion: v1
kind: Service
metadata:
  name: fe
  labels:
    {{- include "fe.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "fe.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: http
